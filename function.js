function sum(num1, num2) {
    var total = num1 + num2;
    console.log(total)
}
// sum(10, 30);


function fullName(first, last) {
    var firstName = first;
    var lastName = last;
    const fullName = firstName + ' ' + lastName;
    return fullName
}


var myName = fullName('Md', 'Sadiq');
console.log(myName)
